/*
 * Header Messages
 *
 * This contains all the text for the Header component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  findAJob: {
    id: 'app.components.Header.findAJob',
    defaultMessage: 'Find a Job',
  },
});
